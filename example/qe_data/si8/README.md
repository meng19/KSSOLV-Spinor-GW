# Shared QE groundstate (Gamma Si)

Used by all four `examples/cases/gamma_*` demos via

```text
groundstate_dir = '../qe.save'
```

Expected contents:

- `data-file-schema.xml`
- `wfc*.hdf5`
- `charge-density.hdf5` (if used)
- `vxc.dat`
